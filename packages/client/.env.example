# Local dev (matches `wrangler dev`'s default port):
VITE_WS_URL=ws://localhost:8787/ws

# Production (set as a Vercel environment variable, not in a committed file):
# VITE_WS_URL=wss://duo-chess.<your-cloudflare-subdomain>.workers.dev/ws
